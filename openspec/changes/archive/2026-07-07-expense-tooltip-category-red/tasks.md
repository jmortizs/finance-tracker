## 1. Tooltip Styling

- [x] 1.1 Update `DistributionTooltipContent` to render expense category names with `text-danger` and income category names with `text-accent`

## 2. Tests

- [x] 2.1 Add unit tests asserting expense tooltip category uses `text-danger`
- [x] 2.2 Add unit tests asserting income tooltip category uses `text-accent`
- [x] 2.3 Run frontend test suite for `DistributionChart`
